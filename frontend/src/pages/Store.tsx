import React, { useState, useEffect } from "react"
import { Typography } from "@mui/material"
/*
const Store: React.FC = () => {
    const [files, setFiles] = useState<any[]>([]) // Placeholder
    
    useEffect(() => {
        const fetchFiles = async () => {
            
        }
    })

    return
}
    */